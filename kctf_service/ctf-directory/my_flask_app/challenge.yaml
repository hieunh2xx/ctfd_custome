apiVersion: kctf.dev/v1
kind: Challenge
metadata:
  name: my-flask-app
spec:
  deployed: true
  powDifficultySeconds: 0
  network:
    public: true
    ports:
      - protocol: "HTTPS"
        targetPort: 1337
  healthcheck:
    # TIP: disable the healthcheck during development
    enabled: false
  image: kind/my-flask-app:challenge
